<!DOCTYPE html>

<title>Hello Admin!</title>

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>ADMIN! ✅</h1>
</div>
    <div>
        <a class="btn btn-sm btn-primary" href="{{ path('app_user') }}">USER'S</a>
    </div>
    <div>
        <a class="btn btn-sm btn-primary" href="{{ path('app_format') }}">BOOK FORMAT</a>
    </div>
    <div>
        <a class="btn btn-sm btn-primary" href="{{ path('app_materijal') }}">BOOK MATERIJAL</a>
    </div>
    <div>
        <a class="btn btn-sm btn-primary" href="{{ path('app_fm') }}">PRICE FOR MATERIJAL AND FORMAT</a>
    </div>
{% endblock %}