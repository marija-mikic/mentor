<!DOCTYPE html>
<head>
<title>{% block title %}first aplication{% endblock %}</title>
{% block stylesheets %}
    <link rel="stylesheet" href="https://bootswatch.com/4/yeti/bootstrap.min.css">
{% endblock %}
</head>
<div>
<a class="nav-link text-black-50" href="/"> Naslovna </a></li>
</div>
 {% if app.user %}
    <div>
        Dobrodošao {{app.user.username }} <a class="btn btn-sm btn-danger" href="{{ path('app_logout') }}">ODJAVA</a>      
    </div>
{% else %}
    <div>
        <a class="nav-link text-black-50" href="{{ path('app_login') }}">PRIJAVA</a>
    </div>
    <div>
         <a class="nav-link text-black-50" <a href="{{ path('app_register') }}">REGISTER </a>
   </div>
{% endif %}
               
{% if is_granted('ROLE_ADMIN') %}
        <div>
            <a class="btn btn-sm btn-primary" href="{{ path('admin') }}">ADMIN</a>
        </div>
{% endif %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>
{% endblock %}
<div class="example-wrapper">
    <h1>Dobar dan ,dobro došli na Marijin web shop! ✅</h1>
</div>
     

